<Window x:Class="Final.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Final.Views"
        xmlns:viewmodels="clr-namespace:Final.ViewModels"
        xmlns:p="clr-namespace:Final.Properties"
        xmlns:views="clr-namespace:Final.Views"
        mc:Ignorable="d"
        Title="Meteo" Height="500" Width="820"
        Icon="/assets/soleil.ico">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:MeteoViewModel}">
            <views:MeteoView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="{x:Static p:traduction.Menu_Configuration}"
                      Command="{Binding OpenConfigurationWindowCommand}"/>
        </Menu>

        <ContentControl Grid.Row="1" Content="{Binding ViewModelActuel}"/>

    </Grid>
</Window>
