<Window x:Class="Final.Views.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Final.Views"
        xmlns:p="clr-namespace:Final.Properties"
        mc:Ignorable="d"
        Title="{x:Static p:traduction.Menu_Configuration}" Height="340" Width="680"
        Icon="/assets/soleil.ico"
        >
    <Grid Margin="12">
        <StackPanel Margin="16">
            <TextBlock Text="{x:Static p:traduction.Jeton}" />
            <TextBox Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12"/>

            <TextBlock Text="{x:Static p:traduction.Langue}" />
            <ComboBox ItemsSource="{Binding Langues}"
              SelectedItem="{Binding LangueSelectionnee}"
              Margin="0,4,0,12"/>

            <TextBlock Text="{x:Static p:traduction.Changement_Langue}" />




            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{x:Static p:traduction.Sauvegarder}" Command="{Binding SaveCommand}" Margin="0,0,8,0"/>
                <Button Content="{x:Static p:traduction.Annuler}" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
